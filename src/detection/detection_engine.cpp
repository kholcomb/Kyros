#include <kyros/detection/detection_engine.hpp>

namespace kyros {

// Base class implementation - mostly pure virtual, no implementation needed
// The base class only provides the helper method make_evidence which is inline

} // namespace kyros
